<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flo_BBangJun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BottomNaviTemplate">
        <activity android:name="com.example.flo_BBangJun.SplahActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.flo_BBangJun.MainActivity"/>
        <activity android:name="com.example.flo_BBangJun.SongActivity"/>// 새로운 바인딩을 할 때 새로운 액티비티를 추가해주어야 함
    </application>

</manifest>